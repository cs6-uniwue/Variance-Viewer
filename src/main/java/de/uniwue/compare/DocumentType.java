package de.uniwue.compare;

/**
 * Document types that are considered when comparing.
 * Every document type not present here is considered as plain text.
 */
public enum DocumentType {
	PLAINTEXT, TEI, XML
}
